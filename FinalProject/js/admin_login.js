//验证登录
function checkLogin() {
	/***
    var fm = document.login;
	if (fm.admin_user.value == '' || fm.admin_user.value.length < 2 || fm.admin_user.value.length > 20) {
		alert('Enter name please.');
		fm.admin_user.focus();
		return false;
	}
	if (fm.admin_pass.value == '' || fm.admin_pass.value.length < 6 ) {
		alert('Enter password please.6 characters at least.');
		fm.admin_pass.focus();
		return false;
	}
	if (fm.code.value.length != 4 ) {
		alert('Code must be 4 characters');
		fm.code.focus();
		return false;
	}
	return true;
    
    ***/
}